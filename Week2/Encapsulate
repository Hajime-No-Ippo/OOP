Public

Meaning: Anyone can use it.
Example: A public method or variable can be accessed from anywhere in your program.

Private

Meaning: Only the class itself can use it.
Example: A private variable can’t be seen or changed by other classes.

Protected

Meaning: Only the class and its subclasses (or classes in the same package) can use it.
Example: A protected method can be used by child classes.
